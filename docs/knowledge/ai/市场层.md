# 市场层 — 宏观市场分析独立子图

> **架构定位**：三层 Subgraph 金字塔的第一层。先于板块层和个股层执行，不依赖 ticker。
>
> **关联文档**：[index.md](../../index.md) | [板块层](板块层.md) | [个股层](个股层.md) | [done/事件研究方案.md](../../requirements/archive/事件研究方案.md) | [done/市场层证据驱动分析与三级事件路由改造方案.md](../../requirements/archive/市场层证据驱动分析与三级事件路由改造方案.md)
>
> **实现状态**：✅ 已实现（证据驱动改造 2026-09-11 完成，见上方关联方案）

---

## 架构

市场层是一个**独立编译的 LangGraph 子图**（`AI/marketAgents/market_layer_graph.py`），与父图共享 `AgentState`。串行拓扑（证据驱动改造后）：

```
START → 国际事件提取 → 国际影响 → 中国新闻 → 中国技术 → Risk Gate（纯代码）→ END
        （预取事件研究）  （全球风险） （资金日历） （技术环境）   （derive_risk_gate）
```

设计原则：**数据先聚合、规则先计算，LLM 仅负责解释与条件化判断**。特征层（`AI/dataflows/market_features.py`）在 LLM 调用前构建可复现、带时点的特征；`risk_gate` 只消费结构化状态，由独立纯代码节点按有序规则表短路判定，不依赖任何 LLM 输出。

## 市场层内部结构

| 层级 | 节点 | 文件 | 结构化输出 | 工具循环 |
|------|-------|------|----------|:---:|
| Layer 0 | 国际事件提取 | `international_event_extraction.py` | `international_events`（事件列表 ≤5 条） | —（预取） |
| Layer 0 | 国际影响 | `international_news_analyst.py` | `global_risk_assessment` | — |
| CN ★ | 中国新闻分析 | `cn_news_analyst.py` | `market_event_calendar`（dict） | ✅ |
| CN ★ | 中国技术分析 | `cn_tech_analyst.py` | `market_regime`（dict） | — |
| — | Risk Gate | `market_layer_graph.py` | `risk_gate`（normal/caution/block） | —（纯代码） |
| US / KR | 新闻/技术 ×4 | `us_*/kr_*_analyst.py` | （停用，数据达标后单独评审） | — |

> 所有 Agent 不依赖 `company_of_interest`，仅使用 `trade_date`（及 `requested_trade_date` / `date_correction` 用于日期校正透明度标注）。
> `COUNTRIES = ["cn"]`：新增国家在 `market_layer_graph.py` 的 `COUNTRIES` 中加一行 + 写两个 Analyst 文件；数据能力与时点验证达标前不得启用（方案第九章/第十章启用门槛）。

## 三级事件路由与预取（2026-09-11）

- 事件库 `events` 表带 `event_scope`（market/sector/stock）+ `affected_scope_refs`（行业申万代码 / 概念东财 dc 代码 / 股票统一后缀），由 **AI 预标注建议 + 审核人确认**写入（两条审核链路：Streamlit + 平台，服务端校验格式/存在性/组合，失败 `REVIEW_ROW_FAILED` 行级重提）。
- 市场/板块/个股信息 Agent 在 LLM 调用前由各自预取器（`international_event_prefetch.py` / `sector_event_prefetch.py` / `stock_event_prefetch.py`）**进程内直连** `review_dao` 与 `predict_impact`：固定窗口（首期 `000300.SH` + `post_event_5d`）、`as_of=event_time` 防前视、每层最多 5 条；失败降级（`no_sample` / `coverage_missing` / `api_unavailable`）不阻塞节点，历史统计不得由 LLM 补写。
- 技术 Agent（CN/板块/个股）不读取任何事件或信息研究报告；板块技术可消费板块新闻主线的结构化摘要做技术确认（板块层约定边界）。

## Layer 0 — 国际事件提取与国际影响

- 国际事件提取：事实识别、分类、历史影响解释与可比性边界；事实只来自输入、仅提取 0–5 个重大事件；输出结构化 `international_events`（事件事实 + 预取历史统计 + 置信度 + 数据质量）。
- 国际影响：事件类证据只消费 `international_events`；风险价格证据来自 `build_global_risk_features`（美债曲线/期限利差、海外股指、USDCNH/黄金；VIX/SOX/DXY/WTI 不可得，用 SPX/IXIC 已实现波动率与国内期货替代并标注）；输出 `global_risk_assessment`（`systemic_risk`/`risk_appetite`/`confidence`/证据/传导/数据质量，枚举见方案第十二章）。
- 事件研究 ToolNode 已移除（不再注册 `search_similar_events` 工具）。

## Layer 1 — 中国市场 (CN) ★

**中国新闻分析**：消费 `build_cn_event_calendar_features`（5/20/60 交易日窗口压力分级：IPO 募资额/解禁市值/交割/两融历史/事件日历），只分析可验证日历、其余显式降级；输出结构化 `market_event_calendar`（dict：三级别 level/score/drivers/key_dates/confidence + data_quality）。

**中国技术分析**：消费 `build_cn_technical_features`（7 指数 MA5/20/60/120/250、斜率、收益、量比；宽度/高标情绪；资金 1/5/20 变化；风格；估值分位；流动性），只使用预计算特征、固定枚举，按"结论→证据→确认条件→失效条件→置信度"输出结构化 `market_regime`（dict：`short_term`/`wave`/`long_term` + `style`/`sentiment_cycle`/`data_quality`）。

**Risk Gate**：独立纯代码节点（CN Tech 之后、接 END）调用 `derive_risk_gate(global_risk_assessment, market_regime, market_data_quality)` 按第十二章有序规则表短路判定（枚举 `normal/caution/block` 不变；数据质量不足最高 `caution`；fail-open 已改 fail-cautious）。

## 数据质量与时点

- `market_data_quality` 由图启动单点写入（`build_data_quality_summary`，无 reducer，节点只读）；节点级缺失内嵌于各结构化输出的 `data_quality` 子字段。
- 全部特征携带 `as_of_date`，晚于 `trade_date` 的数据丢弃并记录 `future_data_detected=True`；lookbacks/windows 均为交易日口径。

## 下游消费

市场层结构化结论（`global_risk_assessment` → `international_events` → `market_regime` → `market_event_calendar`）按优先级注入下游：结构化摘要优先（dict → 紧凑 Markdown 渲染），长文本 report 仅展示或调试；`risk_gate` 供仓位管理消费。板块/个股层各自预取本层作用域事件（`sector_events`/`stock_events`），原始事件不跨层透传。

## 数据源

### 结构化特征接口（2026-09-11 新增，基类契约）

8 个结构化接口定义于 `AI/dataflows/providers/base_provider.py`（默认返回 `None`，结构化例外组），优先 TushareProvider 覆写（`providers/cn/tushare.py`），AKShare 仅在 Tushare 无能力时补缺。POC 实测结论（2026-09-11）：

| 接口 | 立项 | 数据源与降级 |
|------|:---:|------|
| `get_market_index_features` | ✅ | `index_daily` 单日查询，日线降序先升序归一 |
| `get_market_breadth_history` | ✅ | 宽度/高标情绪序列；≥6000 行静默截断守卫 |
| `get_market_fund_flow_history` | ✅ | 资金流多日变化；截断守卫同上 |
| `get_margin_trading_history` | ✅ | `margin`/`margin_detail`（2010 起）；当日 0 行回退最近交易日并记录实际日期 |
| `get_market_valuation` | ✅ | `index_dailybasic` 指数 PE/PB 5 年分位；全 A 仅单日中位数 |
| `get_cn_liquidity_indicators` | ⚠️ 部分 | `shibor`/`shibor_lpr`（约 7 周滞后）；10Y/DR007/MLF/净投放不立项——Shibor 1W 替代 DR007 并标注、M1/M2 方向替代净投放 |
| `get_cn_event_calendar` | ✅ | 资金/宏观日历，交易日聚合 |
| `get_global_risk_indicators` | ⚠️ 部分 | `us_tycr`/`us_trycr`/`us_trltr`、`index_global` 海外股指、`fx_daily`；VIX/SOX/DXY/WTI/铜不立项——已实现波动率 + 国内期货替代并标注 |

### Global — 国际宏观（AKShare 固定路由）

| 函数 | 实现方式 |
|------|----------|
| `get_global_macro_news(curr_date)` | AKShare `stock_telegraph_cls()` 财联社电报，按 `trade_date` 过滤 |
| `get_central_bank_calendar(curr_date)` | 静态 JSON + AKShare `macro_*` 补充 |
| `get_macro_indicators(curr_date)` | AKShare `macro_usa_cpi_yoy()` / `macro_usa_non_farm()` / `macro_china_pmi()` 等 |
| `get_commodity_fx_overview(days=10)` | AKShare 期货 + 现货黄金 + USD/CNY |

### CN — 中国市场

`get_ipo_calendar` / `get_share_unlock_calendar` / `get_futures_expiry_calendar` / `get_market_breadth` / `get_market_fund_flow` 等既有函数保留（AKShare），作为特征层结构化接口之外的展示/快照数据；两融/指数/估值等走上方新结构化接口。

### 占位函数的降级策略

US/KR 占位函数统一返回"数据不可用"；数据不足时统一输出"数据不足，未纳入判断"，**禁止训练知识填充当前事实**（`COUNTRIES = ["cn"]` 下 US/KR 不参与主链路）。

### 交易日历校正（T-1 问题修复）

> **实现日期**：2026-08-11 | **方案归档**：[done/技术指标T-1交易日限制梳理方案.md](../../requirements/archive/技术指标T-1交易日限制梳理方案.md)

**校正规则**：历史日期仅校验交易日；今天 + 交易日 + ≥20:00 不变；否则回退上一交易日。降级链路：Tushare `trade_cal` → AKShare → weekday-only 兜底 → fail-open。

**相关文件**：`AI/dataflows/utils/trading_calendar.py`、`propagation.py`（`create_initial_state` 校正）、`interface.py`（`_correct_trade_date`）。
