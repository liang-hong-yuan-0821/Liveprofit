# 复盘

归档前写。任务级复盘；**跨任务可复用的主题沉淀进 docs/memory/**（按「经验沉淀规则」），本文件只留本任务的视角。

## 做对了什么（可复用）

- **评审与 CR 全部走"真实 echarts SSR/源码实测锚定"**——markLine 嵌套形态、`showContent:false`、legend 数组、containData 边界严格包含、zrender Eventful 共享，全是先实测再落码，测试断言与实测结论一一对应
- **方案评审异常上报机制真实触发一次并有效收敛**（画线 y 裁剪 4 轮处方级残留 → 维度 8 扩充"渲染语义边界必须以真实 extent 锚定"）
- 按引用 off 的修复 + "外来 handler 存活"断言——用单测固化第三方库内部耦合

## 踩了什么坑（教训）

- 两轮 CR 才过的两个 blocker 都是"共享第三方内部状态"类：zrender Eventful 与 ECharts 内部共用（裸 off 误删不可恢复）；测试 mock 的简化语义掩盖真实行为（覆盖 vs 累积）
- y 求交参数化基准取错（终点当起点）——几何类修复必须配 x+y 双坐标断言，只断 y 会被斜率反转骗过

## 下次改进

- 图表类模块的 zr/Eventful 交互，动手前先读 zrender 源码核对注册/去重/清理语义，不要凭"覆盖即重挂"直觉
- 涉及共享底层（Eventful/canvas 原型）的 mock 应保留真实语义（累积/按引用过滤），并预置"外来者"断言
- 测试断言覆盖完整坐标（x+y），不要只断被修的那个维度
