# 事件研究审核界面平台集成方案 任务清单

> **状态**：`任务分解`（2026-09-08）
> **进度**：0/6 任务
> **下一步**：从 T1（后端领域层）开始实现
> **关联方案**：[../plans/事件研究审核界面平台集成方案.md](../plans/事件研究审核界面平台集成方案.md)

---

## 任务总览

| 编号 | 任务 | 依赖 | 状态 |
|------|------|------|------|
| T1 | 后端领域层：contracts/errors/adapter/service + 单测 | — | 已完成 |
| T2 | 后端 API 层：schemas/router/_CODE_MAP/main.py 接线 | T1 | 已完成 |
| T3 | 契约测试 12 用例 + OpenAPI 导出与 FROZEN_PATHS 门禁 | T2 | 已完成 |
| T4 | 前端基础：generate:api + client 超时参数 + queryKeys + mappers | T3 | 进行中 |
| T5 | 前端审核页面：queries + 组件 + 路由入口 + vitest | T4 | 已完成 |
| T6 | 端到端验证 + Code Review + 归档收尾 | T5 | 进行中 |

## 任务

### T1 后端领域层（contracts/errors/adapter/service + 单测）

- **目标**：交付方案 3.1.1 的领域层四件套与单测
- **涉及文件**：
  - 新建：`backend/modules/event_study/application/review_contracts.py`（冻结 dataclass：ReviewRowCommand/ReviewRowResult/ReviewBatchResult/PrelabelResult/ComputeResult/ConfirmImpactsResult/PendingEventDTO/ImpactDraftDTO）
  - 新建：`backend/modules/event_study/application/review_errors.py`（4 个 DomainError 子类）
  - 新建：`backend/modules/event_study/infrastructure/review_adapter.py`（EventStudyReviewAdapter：open_connection/redis_available/list_pending_events/approve/ignore/compute_windows/prelabel/list_impact_drafts/event_title/confirm_impacts，包 AI 侧函数）
  - 新建：`backend/modules/event_study/application/review_service.py`（EventStudyReviewService 六个同步方法；submit_batch：整体预检 → 逐行 review_fields 仅含非 None 键 → 行级捕获 ValueError/Exception 映射 → 失败路径统一 rollback → approve 后同 conn compute）
  - 新建：`backend/tests/unit/event_study/test_review_service.py`（FakeAdapter 注入：行级异常隔离、rollback 委托断言、operator 透传、fields 不含 None 键、DTO 映射、confirm 聚合计数、预检 503）
- **依赖**：无
- **验收标准**（全部勾选才算完成）：
  - [x] `pytest backend/tests/unit/event_study/test_review_service.py -q` 全绿（18 passed，无需真实 PG/Redis/LLM）
  - [x] 人工检查：review_adapter.py 的方法内 import 不触发模块级 AI 依赖加载（导入后 `AI.eventStudy` 模块数为 0）
- **状态**：`已完成`（2026-09-08）

### T2 后端 API 层（schemas/router/_CODE_MAP/main.py 接线）

- **目标**：交付方案 3.1.1 的 6 端点 API 层与接线
- **涉及文件**：
  - 新建：`backend/api/schemas/event_study_review.py`（ReviewRowRequest/ReviewBatchRequest/PrelabelRequest/ComputeRequest/ConfirmImpactsRequest + 各响应模型，Envelope 泛型化）
  - 新建：`backend/api/routers/event_study_review.py`（prefix `/api/v1/event-studies/review`、tags `["event-studies"]`、6 端点，经 `analysis_services.run()` 调服务）
  - 修改：`backend/api/exception_handlers.py`（`_CODE_MAP` 增 4 个 code）
  - 修改：`backend/main.py`（lifespan 装配 `event_study_review_service` + `include_router`）
- **依赖**：T1
- **验收标准**：
  - [x] `pytest backend/tests/contract/api/test_event_studies.py -q` 仍绿（30 passed，接线不破坏既有契约）
  - [x] 人工检查：`create_app()` 装配成功，`app.openapi()` 含 6 条 `/api/v1/event-studies/review/*` 路径（liveprofit-api 进程存活在 T6 端到端验证）
- **状态**：`已完成`（2026-09-08）

### T3 契约测试 12 用例 + OpenAPI 导出与 FROZEN_PATHS 门禁

- **目标**：方案 3.3.1 的契约测试与 OpenAPI 门禁
- **涉及文件**：
  - 新建：`backend/tests/contract/api/test_event_study_review.py`（autouse fixture：monkeypatch 注入 PG_CONNECTION_STRING/REDIS_CONNECTION_STRING/_redis_client + `_ensure_review_tables` + `_truncate_review_tables`；12 个用例按方案 3.3.1 清单）
  - 修改：`backend/tests/contract/test_openapi_gate.py`（`FROZEN_PATHS` 增 6 条）
  - 修改：`backend/openapi/openapi.v1.json`（`python -m backend.scripts.export_openapi` 重导）
- **依赖**：T2
- **验收标准**：
  - [x] `pytest backend/tests/contract/api/test_event_study_review.py -q` 全绿（15 passed）
  - [x] `pytest backend/tests/contract/test_openapi_gate.py -q` 绿（幂等 diff + 冻结路径）
  - [x] `pytest tests/event_study/test_review_regressions.py tests/event_study/test_ai_prelabel.py -q` 仍绿（17 passed，AI 侧零改动回归）
  - [x] 人工检查：重导 openapi.v1.json 后含 6 条 review 路径（幂等门禁保证无既有路径漂移）
- **状态**：`已完成`（2026-09-08）

### T4 前端基础（generate:api + client 超时参数 + queryKeys + mappers）

- **目标**：前端契约与数据映射层
- **涉及文件**：
  - 修改：`frontend/src/api/generated/**`（`pnpm run generate:api` 重生成，审核端点并入 EventStudiesService）
  - 修改：`frontend/src/api/client.ts`（`requestEnvelope(promise, opts?: {timeoutMs})`，默认 45s 不变）
  - 修改：`frontend/src/api/queryKeys.ts`（`eventStudyReview: makeQueryKeys('event-study-review')`）
  - 新建：`frontend/src/modules/event-study/pages/review/mappers/toPendingEventRowVM.ts` / `toImpactRowVM.ts`（importance 链 `suggestions ?? hint ?? 3`、数值空串→null、direction 映射、CAR None→空）
  - 新建：mappers 对应 `*.test.ts`（边界：空串→null、direction `{1,-1,0,undefined}`、默认链）
- **依赖**：T3
- **验收标准**：
  - [ ] `pnpm run typecheck` 通过
  - [ ] `pnpm run test src/modules/event-study/pages/review` 绿（此时仅 mappers 用例）
- **状态**：`待开始`（2026-09-08）

### T5 前端审核页面（queries + 组件 + 路由入口 + vitest）

- **目标**：方案 3.2.1 的审核页面全量
- **涉及文件**：
  - 新建：`frontend/src/modules/event-study/pages/review/queries.ts`（6 个 hook，batch/prelabel/compute 用 `timeoutMs: 300_000`）
  - 新建：`frontend/src/modules/event-study/pages/review/EventStudyReviewPage.tsx` / `PendingEventsTab.tsx` / `PendingEventRow.tsx` / `EventDetailDialog.tsx` / `ImpactConfirmTab.tsx`
  - 新建：`EventStudyReviewPage.test.tsx` / `PendingEventsTab.test.tsx` / `ImpactConfirmTab.test.tsx`（mock 工厂 `'../../../../api/generated/services/EventStudiesService'` 补 6 新方法；用例按方案 3.3.4 清单）
  - 修改：`frontend/src/routes/index.tsx`（`ai/event-study/review` 路由）
  - 修改：`frontend/src/modules/analysis/pages/dashboard/AiDashboardPage.tsx`（「事件审核」入口按钮）
- **依赖**：T4
- **验收标准**：
  - [x] `pnpm run test src/modules/event-study src/modules/analysis/pages/dashboard` 绿（47 passed，含既有用例不回归）
  - [x] `pnpm run build` 通过
  - [x] 人工检查：dev server 下 `/ai/event-study/review` 返回 200（SPA 正常装配，组件渲染由 vitest 覆盖）
- **状态**：`已完成`（2026-09-08）

### T6 端到端验证 + Code Review + 归档收尾

- **目标**：方案 3.4 端到端跑通 + 收尾
- **涉及文件**：
  - 修改：`docs/plans/事件研究审核界面平台集成方案.md`（状态块随进度更新，最终移入 `docs/done/`）
  - 删除：`docs/tasks/事件研究审核界面平台集成方案.md`（归档时删除）
  - 修改：`CLAUDE.md`（沉淀新约定：API 进程内复用 AI 侧全局 config 的注入式测试模式）
- **依赖**：T5
- **验收标准**：
  - [ ] 端到端观察点（按方案 3.4 步骤 1-6）：Redis 种子草稿 → 预填 → 批量通过+二次确认 → Tab2 勾选确认 inserted=3 → PG `events`/`event_impacts` 行与 Redis 草稿删除/`event_review_log` 两条均符合预期；忽略路径与「计算失败·重试」各走一遍
  - [ ] Code Review：subagent 对照方案文件逐文件 review，发现项修复后 re-review 通过（≤2 轮）
  - [ ] 方案归档 `docs/done/`、任务清单删除、CLAUDE.md 沉淀更新
- **状态**：`待开始`（2026-09-08）
