# <方案名> 任务清单

> **状态**：`任务分解`（<最后更新时间>）
> **进度**：0/<总任务数> 任务
> **下一步**：<从哪个任务开始实现>
> **关联方案**：[../plans/<方案名>.md](../plans/<方案名>.md)

---

## 任务总览

| 编号 | 任务 | 依赖 | 状态 |
|------|------|------|------|
| T1 | <一句话任务名> | — | 待开始 |
| T2 | <一句话任务名> | T1 | 待开始 |

## 任务

> 每个任务块按下方「任务块模板」复制填写。实现过程中每完成一个任务：更新该任务块的「状态」、勾选验收项，并同步任务总览表与顶部状态块的进度。

### T1 <任务名>

- **目标**：<本任务要交付什么，对应方案的哪一节>
- **涉及文件**：
  - 新建：`<路径>`（说明）
  - 修改：`<路径>`（改动说明）
- **依赖**：无（或：前置任务 T<n>）
- **验收标准**（全部勾选才算完成）：
  - [ ] <可执行的检查项：pytest 命令 / 可运行检查 / 写明观察点的人工检查>
- **状态**：`待开始`（<更新时间>）

### T2 <任务名>

（同上结构）

---

## 任务块模板（复制用，不保留在正式清单中）

### T<n> <任务名>

- **目标**：
- **涉及文件**：
- **依赖**：
- **验收标准**：
  - [ ]
- **状态**：`待开始`（<更新时间>）

---

## 拆分与维护规则

- **拆分粒度**：每个任务 = 一个可独立验收的实现单元（新建一个模块 / 改造一个文件 / 写一组单测）；按依赖排序，无依赖任务可并行。通常 3–10 个任务，超出说明拆分过细，可合并
- **验收标准必须可执行**：优先单测命令与可运行检查；人工检查需写明看什么、期望看到什么。禁止"完成 XX 功能"式模糊表述
- **状态取值**：`待开始` → `进行中` → `已完成`；被阻塞时标 `阻塞：<原因>`，解除后恢复流转
- **生命周期**：任务清单是过程性文档——方案实现完成并归档（移入 `docs/done/`）时，本文件随之删除（验收结论已固化进方案文件与代码）
- **无需额外评审**：任务清单直接由已评审通过的方案拆出，只做拆解、不复述设计

> **使用方式**：方案经用户确认后，复制本文件到 `docs/tasks/<方案名>.md`（与方案文件同名），删除本提示块与「任务块模板」章节，按模板逐条填写任务块。已有的方案文档不做回填改造，本模板仅约束此后新建的任务清单。
